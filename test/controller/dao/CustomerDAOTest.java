package controller.dao;

public class CustomerDAOTest {
	/*
	 * private static CustomerDAO customerDAO; private static PersonDAO personDAO;
	 * private static AddressDAO addressDAO; private static AccountDAO accountDAO;
	 * private static FullnameDAO fullnameDAO;
	 * 
	 * @BeforeClass public static void setUpBeforeClass() throws Exception {
	 * customerDAO = new CustomerDAO(); personDAO = new PersonDAO(); addressDAO =
	 * new AddressDAO(); accountDAO = new AccountDAO(); fullnameDAO = new
	 * FullnameDAO(); }
	 * 
	 * @AfterClass public static void tearDownAfterClass() throws Exception {
	 * customerDAO.close(); personDAO.close(); addressDAO.close();
	 * accountDAO.close(); fullnameDAO.close(); }
	 * 
	 * @Test public void testCreateCustomer() throws ParseException { Account
	 * account = new Account("test", "user", "test"); Account createdAccount =
	 * accountDAO.create(account);
	 * 
	 * Fullname fullname = new Fullname("Hung", "Ta"); Fullname createdFN =
	 * fullnameDAO.create(fullname);
	 * 
	 * Address address = new Address("PK", "Ha Noi", "VN"); Address createdAddress =
	 * addressDAO.create(address);
	 * 
	 * SimpleDateFormat dtf = new SimpleDateFormat("yyyy-MM-dd"); Date date =
	 * dtf.parse("1998-04-07"); Person person = new Person(date , "test@gmail.com",
	 * "0988333444");
	 * 
	 * person.setAccount(createdAccount); person.setFullname(createdFN);
	 * person.setAddress(createdAddress);
	 * 
	 * Person createdPerson = personDAO.create(person);
	 * 
	 * Customer customer = new Customer(); customer.setPerson(createdPerson);
	 * 
	 * Customer createdCustomer = customerDAO.create(customer);
	 * 
	 * assertNotNull(createdCustomer); }
	 */
}
