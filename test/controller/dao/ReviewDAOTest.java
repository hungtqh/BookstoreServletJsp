package controller.dao;

public class ReviewDAOTest {
	/*
	 * private static ReviewDAO reviewDAO;
	 * 
	 * @BeforeClass public static void setUpBeforeClass() throws Exception {
	 * reviewDAO = new ReviewDAO(); }
	 * 
	 * @AfterClass public static void tearDownAfterClass() throws Exception {
	 * reviewDAO.close(); }
	 * 
	 * @Test public void testCreateReview() { Review review = new Review(); Item
	 * item = new Item(); item.setItemID(14); Customer customer = new Customer();
	 * customer.setCustomerID(2);
	 * 
	 * review.setItem(item); review.setCustomer(customer);
	 * review.setHeadline("Good book!"); review.setRating(5);
	 * review.setComment("Everyone should read this book!");
	 * 
	 * Review createdReview = reviewDAO.create(review);
	 * 
	 * assertTrue(createdReview.getReviewID() > 0); }
	 * 
	 * @Test public void testUpdateReview() { int reviewId = 15; Review review =
	 * reviewDAO.get(reviewId); review.setHeadline("Awesome!"); Review updatedReview
	 * = reviewDAO.update(review);
	 * 
	 * assertEquals(updatedReview.getHeadline(), "Awesome!"); }
	 * 
	 * @Test public void testGetReview() { int reviewId = 15; Review review =
	 * reviewDAO.get(reviewId);
	 * 
	 * assertEquals(review.getReviewID(), reviewId); }
	 * 
	 * @Test public void testDeleteReview() { int reviewId = 15;
	 * reviewDAO.delete(reviewId);
	 * 
	 * Review review = reviewDAO.get(reviewId);
	 * 
	 * assertNull(review); }
	 * 
	 * @Test public void testListAll() { List<Review> reviews = reviewDAO.listAll();
	 * 
	 * assertTrue(reviews.size() == 1); }
	 * 
	 * @Test public void testCount() { long totalReview = reviewDAO.count();
	 * assertEquals(totalReview, 1); }
	 * 
	 * @Test public void testCountReviewByBook() { int bookId = 35; long
	 * totalReviews = reviewDAO.countReviewByBook(bookId);
	 * 
	 * assertEquals(totalReviews, 1); }
	 * 
	 * @Test public void testCountReviewByCustomer() { int customerId = 12; long
	 * totalReviews = reviewDAO.countReviewByCustomer(customerId);
	 * 
	 * assertEquals(totalReviews, 1); }
	 * 
	 * @Test public void testFindByCustomerAndReview() { int customerId = 1; int
	 * itemId = 14;
	 * 
	 * Review review = reviewDAO.findByCustomerAndItem(customerId, itemId);
	 * 
	 * assertNotNull(review); }
	 */
}
